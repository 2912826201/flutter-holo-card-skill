# medium：固定原图＋前景轮廓镭射

运行时只有 source.png、foreground_contour.png、foreground_bloom.png，加共用箔纹。不补背景，不生成运行时前景彩图。

提示词只用于原图对齐的前景遮罩：明确当前方案 medium、源摘要/画布、每个人物/文字/面板含底色/框/效果的保留范围；白色填实保留部分，背景黑色。不要把内部线条当透明洞，不重画字、不生成彩色前景或背景图。使用 height 文档中的语义遮罩原则，但不执行 height 的背景步骤。

验收：任意姿态原图、轮廓、bloom UV 完全一致，无内部位移；背景没有被描线；小字不重画、无双线；关闭光效与原图渲染一致。整卡物理倾斜可单独关闭。不要将无视差误记为缺陷，它是此档要求。
