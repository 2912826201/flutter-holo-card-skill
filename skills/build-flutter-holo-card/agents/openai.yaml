interface:
  display_name: "Build Flutter Holo Card"
  short_description: "Build stable two-layer Flutter holo cards"
  default_prompt: "Use $build-flutter-holo-card to build one repaired background and one source-faithful combined foreground containing every character and card-interface element. Apply white sketch highlights to the complete foreground and integrate the tested Flutter component unless I request asset-only mode."
