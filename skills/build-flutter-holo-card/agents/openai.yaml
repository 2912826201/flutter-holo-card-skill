interface:
  display_name: "Build Flutter Holo Card"
  short_description: "Build Flutter holo cards or aligned assets"
  default_prompt: "Use $build-flutter-holo-card to prepare aligned holographic-card assets and, unless I request asset-only mode, integrate the tested Flutter component."
