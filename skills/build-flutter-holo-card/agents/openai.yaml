interface:
  display_name: "Flutter Holo Card · 三档"
  short_description: "Build height, medium or low Flutter holographic cards"
  default_prompt: "Use $build-flutter-holo-card to build a Flutter holographic card. Route height (default), medium or low independently from asset-only. Preserve source colors, use source-extracted foreground contours where required, validate mode-bound assets and integrate the bundled shared foil material."
