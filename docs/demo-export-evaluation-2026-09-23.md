# Demo 和导出验证 · 2026-09-23

使用此前验收过的卡图素材进行测试，未重新生成图片。测试素材、ZIP 和浏览器截图保存在本地 `.local-tests/20260923-holo/`，未收录在仓库中。

## 范围与默认值

- 未指定等级：三档可选 Demo，初始 height。
- 明确等级：只生成/打包所需等级资源；medium 不要求前景或补全背景，low 不要求任何生图。
- assets-only：跳过组件与 Demo；兼容 asset-only。
- 材质公式和箔纹保持参考实现，组件与 Demo 默认整体光效 .8、箔光 1。

## 自动验证

- 49 项 Python 测试通过，包含单档图片清单、默认三档顺序、同源校验、拒绝未审查资源、不覆盖已有 Demo。
- 19 项 Flutter 渲染回归通过，包含三档关闭新增效果后与参考 Shader 的像素对比。
- Demo analyze 通过；4 项导出测试通过。分别检查三档 ZIP 的 CRC、参数快照、5/3/1 张卡图加共用箔纹、单个 Shader 入口，以及打包前后图片字节相同。
- 完整 Demo 保留全部所需资源和导出源码；从其归档读取资源再次导出组件及完整 Demo 成功。
- 三个组件 ZIP 各自解压到独立 Flutter 消费项目，分析和 Web release 构建通过。
- 完整 Demo ZIP 解压后分析和 Web release 构建通过。

## 浏览器检查

在 Flutter Web 中切换 medium，整体光效调为 .51，点击两个导出按钮。页面分别完成打包并显示已发起下载 holo-medium-component.zip 和 holo-demo.zip。桌面 1280×1050 布局中卡面、档位、滑块、两个导出按钮可用；随后检查了“恢复默认”功能和窄屏布局。ZIP 内容断言使用同一打包函数的测试产物，浏览器下载落盘位置不作为本轮文件断言。

本次仅验证 Web Demo 的下载功能，尚未在原生手机 GPU 上测试。
