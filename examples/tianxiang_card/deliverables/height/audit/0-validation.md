# 三档验证记录 · 2026-09-21

当前卡为用户提供的天鹅 EX；工作画布 1000×1397，height 背景为 1160×1621，原画布位于 (80,112,1000,1397)。资源摘要、方案及各阶段状态见各档 candidate/manifest.json。

## 自动验证

- Python：19 项通过。覆盖三档资源清单、前景原色、已知背景回填、灰色遮罩、偏移线条、棋盘格、背景改色、尺寸、资源缺失、跨档、上游/视觉证据过期、失败后旧资源撤下、失败归一化、只清理登记文件、portable export、同项目两档集成。
- Flutter 模板：15 项通过。三档资源加载数量分别 6/4/2（含箔纹）；源比例与单轴无界；横竖拖动/连续回正；加载失败原图回退和错误通知；快速替换/卸载/多实例；减少动画；景深 0/1/3、宽高比 .2/.715/1/2/5、九方向的投影采样；前景/Alpha 固定与原图像素对照。
- low 使用实际参考箔纹，与未经修改的参考 Shader 在相同原图、uPointer 和 power 下进行像素对照，通过。参考测试是 Flutter 测试渲染器的结果，不是真机 GPU 对照。
- 实卡示例：Widget 测试 1 项通过；宽窄布局、景深 0/1/3、实际六张纹理、触摸及光效关闭。
- Flutter 分析通过；Web debug 构建通过，包含本地 CanvasKit，不依赖 CDN；Wasm dry run 通过，但未作为 Wasm 运行验收。
- Skill quick_validate 和 git diff --check 通过。

## 实际视觉审查

浏览器使用本机 Flutter Web 构建，检查 1280×900 与 390×844。浏览器错误/警告日志为空。动态检查包含悬停、回正、切换档位；静态证据包含关闭物理倾斜和光效的 27 组 height 姿态、轮廓叠加、medium/low 的关闭与激活画面。

height：前景、框字、外边界固定，背景后退；未再出现旧版整层出框的第二张卡边界。中立画面采用源图加背景差值合成，避免遮罩边缘重复滤波。极限采样留在延展范围内，参见 final-height-contact-sheet.jpg 与 poses/final-height-*.jpg。对输入蒙版的绿机器人内部、印刷面板、细框、动作条纹作了本卡专用修正，过程记录在 prepare_demo.py。已知背景严格回填；未知部分经颜色衔接，仍是生成的推断内容。

medium：原图、源图提取线条和两级 bloom 无内部错位；文字字形没有重绘，轮廓叠加与原画对齐。low：没有分层或线条，仅原图和参考箔纹。三档均保留材料角度变化。

全强度 1.0 的参考材质在这张亮色卡上反射很强，保存了 medium-full-reference-strength.jpg 作为调参证据。默认选 0.65 以保留文字与亮部，仍允许用户查看 1.0 的参考全强度。视觉通过范围是这里记录的浏览器尺寸、默认强度及检查姿态，不宣称所有设备/缩放比都不存在摩尔纹。

## 原生状态

Flutter Widget/raster 测试运行于主机测试环境。macOS 应用、iOS 真机、Android 真机均未进行运行验收；未记录帧率、显存或耗电结论。浏览器通过不代表这些平台通过。

## 旧基线与失败产物

qa/ 根目录保留旧截图、失败 AI 线稿及旧报告；qa/v2/raw-background.png 保留这轮首次补绘残留卡框的失败样例；height-depth3-before-repair.jpg 保留细框/动作条纹蒙版不足的问题证据。后续补全使用 clean-background.png；被新任务替代的候选在各档 diagnostics，未导入最终交付。演示 pubspec 仅注册原图、三档候选运行时图片和显式的轮廓检查图，不打包旧版失败资源。
